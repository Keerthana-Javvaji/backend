# Application name
spring.application.name=CICD

# Server port (Render provides PORT automatically)
server.port=${PORT:9092}

# Database configuration using Render environment variables
SPRING_DATASOURCE_URL=jdbc:postgresql://<host>:5432/mybackenddb
SPRING_DATASOURCE_USERNAME=mybackenddb_user
SPRING_DATASOURCE_PASSWORD=uUe99VGWpJRXcKT7lJnvUHw6GPBndiYK
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
